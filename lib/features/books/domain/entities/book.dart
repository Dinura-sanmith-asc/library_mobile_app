class Book {
  final int id;
  final String title;
  final String author;
  final bool isAvailable;

  const Book({
    required this.id,
    required this.title,
    required this.author,
    required this.isAvailable,
  });
}