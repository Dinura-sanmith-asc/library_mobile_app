## Remaining tasks

- [ ] Finish the UI/UX polish across all screens: use consistent spacing,
  typography, buttons, cards, and responsive layouts for different screen
  sizes.
- [ ] Improve loading, empty, and error states. In particular, add retry or
  refresh actions to the books list, book details, and edit-profile screens.
- [ ] Check accessibility, including text scaling, color contrast, touch-target
  sizes, keyboard behavior, and screen-reader labels.
- [ ] Test every main journey against the real backend: login, session restore,
  browse/search, borrow, return, edit profile, and logout.
- [ ] Add widget tests for the books, book-details, profile, and edit-profile
  screens, plus an integration test for the complete member journey.
- [ ] Prepare release configuration: replace the example application IDs and
  app name, add final launcher/branding assets, configure production API
  settings, require HTTPS, and remove Android cleartext traffic support.
- [ ] Verify Android and iOS release builds on physical devices before
  publishing.
